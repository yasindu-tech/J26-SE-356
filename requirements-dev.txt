# Shared Python dev environment for backend + all model modules.
# Module-specific heavy dependencies live in models/<module>/requirements.txt
# so that a gait developer does not need to install DIPY.

# --- core ---
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

# --- modelling ---
lightgbm>=4.3
shap>=0.45

# --- backend ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-multipart>=0.0.9

# --- quality ---
pytest>=8.2
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
nbstripout>=0.7
