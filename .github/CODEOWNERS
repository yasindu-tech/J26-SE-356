# CODEOWNERS — J26-SE-356
#
# Auto-requests the right reviewer when a PR touches someone's module.
#
# NOTE: "Require review from Code Owners" is intentionally OFF in our branch
# protection, because self-merge is allowed (see CLAUDE.md §5, and the
# open-pr Claude skill for the full PR workflow). So this file *notifies* the
# right person — it does not block the merge. That is the intended behaviour:
# the module owner sees the PR, the author isn't stuck waiting.
#
# ⚠️ Replace the placeholder GitHub usernames below with real handles. An
#    unmatched username silently matches nobody, so the owner never gets asked.
#
#   @yasindu-tech        D.G.Y. Sulochana  IT23267718  (MRI, team lead)
#   @nethal17            Nethal            —           (reviewer, every PR)
#   @<fernando-handle>   Fernando W.S.N.J. IT23259102  (gait)
#   @<ricky-handle>      Ricky H.P.C.      IT23294134  (voice)
#   @<weerasinghe-handle> Weerasinghe P.N.M. IT23247918 (tapping)
#
# @nethal17 and @yasindu-tech are on the catch-all (`*`) below, so both are
# auto-requested as reviewers on every PR that touches any file — which is
# effectively all of them. GitHub automatically skips whichever of the two
# is the PR's own author, so this doesn't need per-author branching logic.
# For this to actually fire, @nethal17 needs at least write access to the
# repo — add as a collaborator under Settings → Collaborators if not done
# already, or GitHub silently can't request the review.

# Default — team lead and Nethal review anything not otherwise owned
*                           @yasindu-tech @nethal17

# Per-module ownership
/models/mri/                @yasindu-tech
/models/gait/               @<fernando-handle>
/models/voice/              @<ricky-handle>
/models/tapping/            @<weerasinghe-handle>

# Shared surfaces — everyone depends on these, lead must review
/models/common/             @yasindu-tech
/models/fusion/             @yasindu-tech
/services/backend/          @yasindu-tech
/packages/shared/           @yasindu-tech

# Apps
/apps/mobile/               @yasindu-tech
/apps/desktop/              @yasindu-tech

# Repo governance — protect the rules themselves from casual edits
/CLAUDE.md                  @yasindu-tech
/README.md                  @yasindu-tech
/.github/                   @yasindu-tech
/.gitignore                 @yasindu-tech
